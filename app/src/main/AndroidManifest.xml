<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tonimiquelllullamengual.teatre_mallorca">

    <application
        android:allowBackup="true"
        android:icon="@drawable/theater"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Teatre Mallorca"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PlacesOcupacy"
            android:label="Pati de butaques" />
        <activity
            android:name=".NewShow"
            android:label="Nova obra" />
        <activity
            android:name=".NewShowDates"
            android:label="Selecció de dates" />
        <activity
            android:name=".ShowList"
            android:label="Llistat obres" />
        <activity
            android:name=".ConfirmPurchase"
            android:label="Confirmar la compra" />
        <activity
            android:name=".DeleteList"
            android:label="Eliminar obra" />
        <activity
            android:name=".InfoShow"
            android:label="Info obra" />
        <activity
            android:name=".DaysList"
            android:label="Llista de funcions" />
        <activity
            android:name=".UsersList"
            android:label="Usuaris" />
        <activity
            android:name=".Help"
            android:label="Help" />
        <activity
            android:name=".Statistics"
            android:label="Estadistiques" />
        <activity
            android:name=".ModifyDates"
            android:label="Modificar dates" />
    </application>

</manifest>
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tonimiquelllullamengual.teatre_idi_nav_bar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OcupacioButaques" />
        <activity android:name=".NovaObra" />
        <activity android:name=".NovaObraDates" />
        <activity android:name=".LlistarObres" />
        <activity android:name=".ComprarEntrades" />
        <activity android:name=".EliminarObra" />
        <activity android:name=".InfoObra"></activity>
    </application>

</manifest>